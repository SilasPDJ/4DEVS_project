from random import sample, choice, randint, shuffle
# img_inuse = os.path.abspath('imagens/'+choice(os.listdir(img_path)))
# usuario = '_'.join(datas['nome'].split()[1:]).lower() + str(datas['numero'])
# title = usuario[::choice([1, -1])].title() + f'{e}:04d'
# title = sample(title, len(title) - 1)

# https://stackoverflow.com/questions/12798363/what-is-the-difference-between-random-sample-and-random-shuffle-in-python#:~:text=There%20are%20two%20major%20differences,bytearray%2C%20set%2C%20etc).
# difference between random [shuffle and sample]
